<%= render 'shared/content_title', title: "Tags"%>

<table class="table">
  <thead>
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Tickets</th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
    <% @tags.each do |tag| %>
      <%= render 'tag', tag: tag %>
    <% end %>
  </tbody>
</table>