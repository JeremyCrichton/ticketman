<tr>
  <th scope="row"><%= link_to ticket.name, ticket_path(ticket[:id]) %></th> 
  <td><%= ticket.status %></td>
  <td><%= list_ticket_tags(ticket) %></td>
  <td>[<%= link_to "delete", ticket_path(ticket[:id]), method: 'delete' %>]</td>
</tr>